---
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import Layout from '@layouts/Default.astro';
import { Card } from '@eliancodes/brutal-ui';
import KoFiCard from '@components/generic/KoFiCard.astro';

export async function getStaticPaths() {
  const blogEntries = await getCollection('blog');
  return blogEntries.map((blogpost) => ({
    params: { slug: blogpost.slug },
    props: { blogpost },
  }));
}

interface Props {
  blogpost: CollectionEntry<'blog'>;
}

const { blogpost } = Astro.props;
const { Content } = await blogpost.render();
const { title, description, author } = blogpost.data;
const pubDate = new Date(blogpost.data.pubDate);
---

<Layout title={title} description={description}>
  <main class="bg-#e4a6ca p-6">
    <article class="max-w-4xl mx-auto">
      <Card color="#ffffff">
        <header class="mb-8">
          <h1 class="text-3xl md:text-5xl lg:text-6xl mb-4 dm-serif">{title}</h1>
          <div class="flex gap-4 text-sm poppins text-gray-600 mb-4">
            <time datetime={pubDate.toISOString()}>
              {pubDate.toLocaleDateString('es-ES', {
                year: 'numeric',
                month: 'long',
                day: 'numeric'
              })}
            </time>
            <span>•</span>
            <span>{author}</span>
          </div>
          <div class="bg-gray-50 p-4 rounded-lg border-2 border-black mb-6">
            <p class="poppins text-lg italic text-gray-700">{description}</p>
          </div>
        </header>

        <div class="prose prose-lg max-w-none poppins">
          <Content />
        </div>

          <div style="margin-bottom: 2rem;">
            <form id="commentForm" style="display: flex; flex-direction: column; gap: 1.25rem;">
              <input type="hidden" name="access_key" value="ac372efb-3a75-42da-9aa0-a90846659aa4">
              <input type="hidden" name="subject" value="New comment on blog">
              <input type="hidden" name="from_name" value="Blog Maaz-Peaches">
              <input type="hidden" name="redirect" value="false">

              <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                <label style="font-size: 0.9rem; font-weight: 600; color: #9f7091; font-family: 'Poppins', sans-serif;" for="name">Name</label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  placeholder="Your name (or leave empty for anonymous)"
                  style="padding: 0.75rem 1rem; border: 2px solid #f5d4e6; border-radius: 0.75rem; font-size: 0.95rem; font-family: 'Poppins', sans-serif; background: #ffffff; color: #9f7091; transition: all 0.3s ease;"
                />
              </div>

              <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                <label style="font-size: 0.9rem; font-weight: 600; color: #9f7091; font-family: 'Poppins', sans-serif;" for="comment">Comment</label>
                <textarea
                  id="comment"
                  name="comment"
                  rows="4"
                  placeholder="Write your comment here..."
                  required
                  style="padding: 0.75rem 1rem; border: 2px solid #f5d4e6; border-radius: 0.75rem; font-size: 0.95rem; font-family: 'Poppins', sans-serif; background: #ffffff; color: #9f7091; transition: all 0.3s ease;"
                ></textarea>
              </div>

              <button type="submit" style="display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem; padding: 0.875rem 1.75rem; background: linear-gradient(135deg, #e8b4d4 0%, #f5d4e6 100%); color: #ffffff; border: none; border-radius: 0.75rem; font-size: 0.95rem; font-weight: 600; font-family: 'Poppins', sans-serif; cursor: pointer; transition: all 0.3s ease; box-shadow: 0 4px 12px rgba(232, 180, 212, 0.4);">
                <span>Send comment</span>
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <line x1="22" y1="2" x2="11" y2="13"></line>
                  <polygon points="22 2 15 22 11 13 2 9 22 2"></polygon>
                </svg>
              </button>
            </form>
          </div>

          <div id="commentsList" style="margin-top: 2rem; padding-top: 2rem; border-top: 2px dashed #f5d4e6;">
            <p style="text-align: center; color: #b89aa8; font-family: 'Poppins', sans-serif; font-size: 1rem; font-style: italic; margin: 1rem 0;">Be the first to comment</p>
          </div>
        
        <KoFiCard username="rollingpeaches" />

        <div class='flex justify-center mt-6'>
          <img 
            src='https://raw.githubusercontent.com/MarinaLopez98/Maaz-Peaches/master/src/assets/Peach.jpg' 
            alt='Ilustración Peach' 
            class='rounded-lg border-2 border-black shadow-lg'
            style='max-width: 300px; width: 100%;'
            loading='lazy'
          />
        </div>
        
      </Card>
    </article>
  </main>
</Layout>

<script>
  console.log('Comments system loaded');

  type Comentario = {
    nombre: string;
    texto: string;
    fecha: string;
  };

  // Simple comment system
  (function() {
    console.log('Setting up comments...');

    const form = document.getElementById('commentForm') as HTMLFormElement | null;
    const commentsList = document.getElementById('commentsList') as HTMLDivElement | null;

    if (!form || !commentsList) {
      console.error('Elements not found');
      return;
    }

    console.log('System ready');

    // Load existing comments
    loadComments();

    // Handle form submission
    form.addEventListener('submit', function(e: Event) {
      e.preventDefault();
      console.log('Form submitted');

      const nombre = (document.getElementById('name') as HTMLInputElement | null)?.value?.trim() || 'Anonymous';
      const texto = (document.getElementById('comment') as HTMLTextAreaElement | null)?.value?.trim();

      if (!texto) {
        alert('Please write a comment');
        return;
      }

      // Create comment object
      const comentario: Comentario = {
        nombre: nombre,
        texto: texto,
        fecha: new Date().toISOString()
      };

      // Show immediately with PINK text for main comments
      showComment(comentario);

      // Save to localStorage
      saveComment(comentario);

      // Clear form
      form.reset();

      // Send email
      sendEmail(comentario);

      console.log('Comment processed');
    });

    function loadComments(): void {
      const clave = 'comments_' + window.location.pathname;
      const comentarios: Comentario[] = JSON.parse(localStorage.getItem(clave) || '[]');

      if (!commentsList) {
        return;
      }

      if (comentarios.length > 0) {
        commentsList.innerHTML = '';
        comentarios.forEach(showComment);
      }
    }

    function showComment(comentario: Comentario): void {
      const div = document.createElement('div');
      div.className = 'comment-item';

      const fecha = new Date(comentario.fecha);
      const fechaTexto = fecha.toLocaleDateString('es-ES', {
        year: 'numeric',
        month: 'short',
        day: 'numeric',
        hour: '2-digit',
        minute: '2-digit'
      });

      // Simple HTML without complex JavaScript
      div.innerHTML = '<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.75rem; padding-bottom: 0.75rem; border-bottom: 1px solid #f5d4e6;"><span style="font-weight: 600; color: #c1899f; font-family: \'Poppins\', sans-serif;">' + escapeHtml(comentario.nombre) + '</span><span style="font-size: 0.85rem; color: #b89aa8; font-family: \'Poppins\', sans-serif;">' + fechaTexto + '</span></div><p style="color: #6d5a68; font-family: \'Poppins\', sans-serif; line-height: 1.6; margin: 0;">' + escapeHtml(comentario.texto) + '</p>';

      div.style.cssText = 'padding: 1.25rem; margin-bottom: 1rem; background: #ffffff; border: 2px solid #f5d4e6; border-radius: 1rem; transition: all 0.3s ease;';

      const commentsContainer = document.getElementById('commentsList') as HTMLDivElement | null;
      if (commentsContainer) {
        commentsContainer.appendChild(div);
      }
    }

    function saveComment(comentario: Comentario): void {
      const clave = 'comments_' + window.location.pathname;
      const comentarios: Comentario[] = JSON.parse(localStorage.getItem(clave) || '[]');
      comentarios.push(comentario);
      localStorage.setItem(clave, JSON.stringify(comentarios));
    }

    function escapeHtml(text: string): string {
      return text
        .replace(/&/g, "&amp;")
        .replace(/</g, "&lt;")
        .replace(/>/g, "&gt;")
        .replace(/"/g, "&quot;")
        .replace(/'/g, "&#039;");
    }

    function sendEmail(comentario: Comentario): void {
      console.log('Sending email...');

      const formData = new FormData();
      formData.append('access_key', 'ac372efb-3a75-42da-9aa0-a90846659aa4');
      formData.append('subject', 'New comment on blog');
      formData.append('from_name', 'Blog Maaz-Peaches');
      formData.append('name', comentario.nombre);
      formData.append('comment', comentario.texto);
      formData.append('page_url', window.location.href);
      formData.append('redirect', 'false');

      fetch('https://api.web3forms.com/submit', {
        method: 'POST',
        body: formData
      })
      .then(response => {
        console.log('Email sent');
        return response.json();
      })
      .then(data => console.log('Email response:', data))
      .catch(error => {
        console.warn('Email error:', error);
      });
    }

    console.log('Comments system ready');
  })();

  // Simple global function
  (window as typeof window & { showReplyForm: () => void }).showReplyForm = function() {
    const replyDiv = document.getElementById('reply-form');
    if (replyDiv) {
      replyDiv.style.display = replyDiv.style.display === 'none' ? 'block' : 'none';
    }
  };
</script>
